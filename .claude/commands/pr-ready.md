Read and follow `.agents/skills/pr-ready/SKILL.md`. Treat `$ARGUMENTS` as
pull-request readiness context. Do not rewrite commits or update a remote
branch without the explicit approvals required by the skill.
