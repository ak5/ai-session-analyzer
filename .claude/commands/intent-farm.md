Read and follow `.agents/skills/intent-farm/SKILL.md`. Treat `$ARGUMENTS` as
the requested evidence scope. Keep candidates in `tmp/intent-farm/` unless the
maintainer explicitly accepts tracked ADR changes.
