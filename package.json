{
  "name": "ai-session-analyzer",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:e2e": "pnpm build && vitest run -c vitest.e2e.config.ts",
    "e2e:setup": "tsx scripts/e2e-setup.ts",
    "asa": "node packages/cli/bin/asa.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
